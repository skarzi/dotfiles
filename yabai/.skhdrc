# Hyper is tilde on keyboard Mac.
# Restart yabai.
fn + lshift - r : launchctl kickstart -k "gui/${UID}/homebrew.mxcl.yabai"
# Open terminal - `fn + enter`.
fn - 0x4C : /Applications/Alacritty.app/Contents/MacOS/alacritty
# TODO(skarzi): Open spotlight.
# fn - d : /Applications/Alacritty.app/Contents/MacOS/alacritty
# Windows (panes).
# Focus
fn - h : yabai -m window --focus west
fn - j : yabai -m window --focus south
fn - k : yabai -m window --focus north
fn - l : yabai -m window --focus east
# Kill current window.
fn + lshift - q : yabai -m window --close
# Swap windows left/right.
fn - 0x1E : yabai -m window --swap east  # 0x1E is the "]" key
fn - 0x21 : yabai -m window --swap west  # 0x21 is the "[" key
# Resize.
fn + lshift - h : yabai -m window --resize left:-10:0
fn + lshift - j : yabai -m window --resize bottom:0:10
fn + lshift - k : yabai -m window --resize top:0:-10
fn + lshift - l : yabai -m window --resize right:10:0
# Toggle fullscreen.
fn - f : yabai -m window --toggle zoom-fullscreen
# Inserting mode.
fn - v : yabai -m window --insert south  # Vertical.
fn - g : yabai -m window --insert east  # Horizontal.
# Workspaces.
# fn - 1 : yabai -m space --focus 1
# fn - 2 : yabai -m space --focus 2
# fn - 3 : yabai -m space --focus 3
# fn - 4 : yabai -m space --focus 4
# fn - 5 : yabai -m space --focus 5
# fn - 6 : yabai -m space --focus 6
# fn - 7 : yabai -m space --focus 7
# fn - 8 : yabai -m space --focus 8
# fn - 9 : yabai -m space --focus 9
# fn - 0 : yabai -m space --focus 10
# Increase window size.
# shift + alt - a : yabai -m window --resize left:-20:0
# shift + alt - s : yabai -m window --resize bottom:0:20
# shift + alt - w : yabai -m window --resize top:0:-20
# shift + alt - d : yabai -m window --resize right:20:0
# Decrease window size.
# shift + cmd - a : yabai -m window --resize left:21:0
# shift + cmd - s : yabai -m window --resize bottom:0:-20
# shift + cmd - w : yabai -m window --resize top:0:20
# shift + cmd - d : yabai -m window --resize right:-20:0
# Move active window to next space on current display.
shift + lalt + lcmd + ctrl + ralt - 1 : yabai -m query --spaces --space | jq -re ".index" | xargs -I {} bash -c "if [[ '{}' = '1' ]]; then yabai -m window --space 2; elif [[ '{}' = '2' ]]; then yabai -m window --space 1; fi"
shift + lalt + lcmd + ctrl + ralt - 2 : yabai -m query --spaces --space | jq -re ".index" | xargs -I {} bash -c "if [[ '{}' = '3' ]]; then yabai -m window --space 4; elif [[ '{}' = '4' ]]; then yabai -m window --space 3; fi"
shift + lalt + lcmd + ctrl + ralt - 3 : yabai -m query --spaces --space | jq -re ".index" | xargs -I {} bash -c "if [[ '{}' = '5' ]]; then yabai -m window --space 6; elif [[ '{}' = '6' ]]; then yabai -m window --space 5; fi"
