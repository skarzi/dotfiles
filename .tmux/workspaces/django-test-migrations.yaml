---
session_name: django-test-migrations
start_directory: /home/luki/dev/github_projects/django-test-migrations
windows:
  - window_name: dev
    focus: true
    layout: 2dd1,198x143,0,0{106x143,0,0,1,91x143,107,0,2}
    options:
      automatic-rename: false
    panes:
      - focus: true
        shell_command:
          - workon django-test-migration
          - clear
          - eval "$(ssh-agent -s)" && ssh-add ~/.ssh/github_rsa
      - shell_command:
          - workon django-test-migration
          - clear
  - window_name: tests
    layout: d900,318x79,0,0,3
    suppress_history: false
    options:
      automatic-rename: false
    panes:
      - focus: true
        shell_command:
          - workon django-test-migrations
          - clear
          - >-
              # grep --include="*.py" -Rin 'needle' .
      - shell_command:
          - workon django-test-migrations
          - clear
          - >-
              # make test
