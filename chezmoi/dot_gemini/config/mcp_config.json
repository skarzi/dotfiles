{
  "mcpServers": {
    "MCP_DOCKER": {
      "command": "docker",
      "type": "stdio",
      "args": [
        "mcp",
        "gateway",
        "run",
        "--profile",
        "default"
      ]
    }
  }
}
